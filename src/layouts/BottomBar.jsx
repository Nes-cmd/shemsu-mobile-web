import { Link, useLocation } from 'react-router-dom';

import { DefaultNav } from './BottomButtons'

const Bottom = (props) => {
    
    return (
        <div className="bar-container">
            <DefaultNav />
        </div>
    )
}
export default Bottom;